<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zerocoders">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zerocoders">
        <activity android:name=".enroll_user"></activity>
        <activity android:name=".bloodrequest" />
        <activity android:name=".find_donor_page" />
        <activity android:name=".bloodrequest" />
        <activity android:name=".find_donor_page" />
        <activity android:name=".update_profile" />
        <activity android:name=".profile_page" />
        <activity android:name=".homepage" />
        <activity android:name=".sign_up_page" />
        <activity android:name=".log_in_page" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>